<% var users = typeof users !== 'undefined' ? users : []; %>
<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Registro de Usuario</title>
    <link href="/css/tailwind.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body
    class="bg-gradient-to-tr from-blue-50 via-white to-indigo-50 min-h-screen flex items-center justify-center p-4 font-['Inter']"
  >
    <div
      class="bg-white/90 backdrop-blur-xl p-8 sm:p-12 rounded-3xl shadow-xl w-full max-w-md border border-gray-100"
    >
      <header class="mb-10 text-center">
        <div
          class="bg-blue-100 w-16 h-16 rounded-2xl flex items-center justify-center mx-auto mb-4"
        >
          <svg
            class="w-8 h-8 text-blue-600"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"
            />
          </svg>
        </div>
        <h2
          class="text-3xl font-bold bg-gradient-to-r from-blue-600 to-indigo-600 bg-clip-text text-transparent mb-2"
        >
          Crear Cuenta
        </h2>
        <p class="text-gray-500">Ingresa tus datos para comenzar</p>
      </header>

      <form
        id="registrationForm"
        action="/registro"
        method="POST"
        class="space-y-6"
      >
        <div class="space-y-4">
          <div>
            <label
              for="name"
              class="block text-sm font-medium text-gray-700 mb-1"
              >Nombre completo</label
            >
            <input
              type="text"
              id="name"
              name="name"
              class="form-input block w-full rounded-xl border-gray-200 bg-gray-50/50 p-3 text-sm transition duration-200 ease-in-out focus:border-blue-500 focus:ring-2 focus:ring-blue-200 focus:bg-white"
              placeholder="Ej. Juan Perez"
            />
            <span
              id="nameError"
              class="text-red-500 text-xs mt-1 hidden"
            ></span>
          </div>

          <div>
            <label
              for="email"
              class="block text-sm font-medium text-gray-700 mb-1"
              >Correo electrónico</label
            >
            <div class="relative">
              <input
                type="email"
                id="email"
                name="email"
                required
                class="form-input block w-full rounded-xl border-gray-200 bg-gray-50/50 p-3 text-sm transition duration-200 ease-in-out focus:border-blue-500 focus:ring-2 focus:ring-blue-200 focus:bg-white"
                placeholder="tu@email.com"
              />
              <div class="absolute inset-y-0 right-0 flex items-center pr-3">
                <svg
                  class="w-5 h-5 text-gray-400"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
                  />
                </svg>
              </div>
            </div>
            <span
              id="emailError"
              class="text-red-500 text-xs mt-1 hidden"
            ></span>
          </div>

          <div class="grid grid-cols-1 gap-4 sm:grid-cols-2">
            <div>
              <label
                for="password"
                class="block text-sm font-medium text-gray-700 mb-1"
                >Contraseña</label
              >
              <input
                type="password"
                id="password"
                name="password"
                required
                class="form-input block w-full rounded-xl border-gray-200 bg-gray-50/50 p-3 text-sm transition duration-200 ease-in-out focus:border-blue-500 focus:ring-2 focus:ring-blue-200 focus:bg-white"
                placeholder="********"
              />
              <span
                id="passwordError"
                class="text-red-500 text-xs mt-1 hidden"
              ></span>
            </div>
            <div>
              <label
                for="confirmPassword"
                class="block text-sm font-medium text-gray-700 mb-1"
                >Confirmar</label
              >
              <input
                type="password"
                id="confirmPassword"
                name="confirmPassword"
                required
                class="form-input block w-full rounded-xl border-gray-200 bg-gray-50/50 p-3 text-sm transition duration-200 ease-in-out focus:border-blue-500 focus:ring-2 focus:ring-blue-200 focus:bg-white"
                placeholder="*********"
              />
              <span
                id="confirmPasswordError"
                class="text-red-500 text-xs mt-1 hidden"
              ></span>
            </div>
          </div>

          <div>
            <label
              for="userType"
              class="block text-sm font-medium text-gray-700 mb-1"
              >Tipo de cuenta</label
            >
            <select
              id="userType"
              name="userType"
              class="form-select block w-full rounded-xl border-gray-200 bg-gray-50/50 p-3 text-sm transition duration-200 ease-in-out focus:border-blue-500 focus:ring-2 focus:ring-blue-200 focus:bg-white"
            >
              <option value="cliente">Cliente</option>
              <option value="administrador">Administrador</option>
            </select>
          </div>
        </div>

        <button
          type="submit"
          class="w-full bg-gradient-to-r from-blue-600 to-indigo-600 text-white font-medium py-3 px-4 rounded-xl shadow-lg shadow-blue-500/25 hover:shadow-blue-500/40 transform transition duration-200 ease-in-out hover:-translate-y-0.5 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2"
        >
          Crear cuenta
        </button>
      </form>

      <div
        id="successMessage"
        class="hidden mt-4 p-3 rounded-lg bg-green-50 text-green-700 text-sm text-center"
      >
        ¡Registro completado con éxito!
      </div>

      <footer class="mt-8 text-center text-sm text-gray-500">
        ¿Ya tienes cuenta?
        <a href="#" class="text-blue-600 hover:text-blue-700 font-medium"
          >Iniciar sesión</a
        >
      </footer>
      <!-- Botón para abrir el modal -->
      <button
        type="button"
        id="showUsersBtn"
        class="mt-4 w-full bg-white border border-blue-200 text-blue-700 font-medium py-2 px-4 rounded-xl shadow hover:bg-blue-50 transition"
      >
        Ver usuarios registrados
      </button>

      <!-- Modal -->
      <div
        id="usersModal"
        class="fixed inset-0 z-50 flex items-center justify-center bg-black/40 hidden"
      >
        <div
          class="bg-white rounded-2xl shadow-xl w-full max-w-lg p-6 relative"
        >
          <button
            id="closeModalBtn"
            class="top-3 right-3 text-gray-400 hover:text-blue-600 text-2xl"
          >
            &times;
          </button>
          <h3 class="text-xl mt-6 font-bold mb-4 text-blue-700">
            Usuarios registrados
          </h3>
          <div class="overflow-x-auto">
            <table class="min-w-full text-sm text-left">
              <thead>
                <tr>
                  <th class="py-2 px-3 border-b font-semibold">Nombre</th>
                  <th class="py-2 px-3 border-b font-semibold">Correo</th>
                  <th class="py-2 px-3 border-b font-semibold">Tipo</th>
                </tr>
              </thead>
              <tbody>
                <% if (users && users.length > 0) { %> <%
                users.forEach(function(user) { %>
                <tr>
                  <td class="py-2 px-3 border-b"><%= user.name %></td>
                  <td class="py-2 px-3 border-b"><%= user.email %></td>
                  <td class="py-2 px-3 border-b capitalize">
                    <%= user.userType %>
                  </td>
                </tr>
                <% }) %> <% } else { %>
                <tr>
                  <td colspan="3" class="py-4 text-center text-gray-400">
                    No hay usuarios registrados.
                  </td>
                </tr>
                <% } %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>

    <script src="/js/registro.js"></script>
  </body>
</html>
